{{!-- Topo com o nome e o ícone animado --}}
<div class="container containerForum mt-4 text-light w-100">
    <div>
        <span class="material-symbols-outlined iconForum">
            forum
        </span>
    </div>
    <div>
        <h3>ECHO Forúm</h3>
    </div>
</div>
<div class="container mt-4">
    <form action="" method="get">
        <div class="card cardSTopic d-flex w-100 p-3">
            <div class="card-body">
                <div class="mb-3">
                    <label for="searchTopic">Buscar por tópico</label>
                    <input type="text" name="searchTopic" id="searchTopic" class="form-control">
                </div>
                <button class="iconSearch" type="submit">
                    <span class="material-symbols-outlined iconSearchTopic">
                        search
                    </span>
                </button>
            </div>
            <div>
                <button type="button" onclick="ativeChat()" class="btn btn-success">Novo Tópico?</button>
            </div>
        </div>
    </form>
    <div class="container containerMsgForm" id="containerMsgForum">
        <form action="/admin/forumAdmin" method="post">
            <div class="card cardMsg p-3">
                <div class="card-body">
                    <div class="mb-3">
                        <label for="topic">Escreva o Tópico</label>
                        <input type="text" placeholder="Assunto da mensagem..." class="form-control" name="titulo" id="msg">
                    </div>
                    <div class="mb-3">
                        <label for="msg">Escreva a mensagem</label>
                        <textarea name="conteudo" class="form-control" placeholder="Mensagem..." id="msg"></textarea>
                    </div>
                </div>
                <div class="w-100 d-flex justify-content-center">
                    <button type="submit" class="btn btn-success">Enviar</button>
                </div>
            </div>
        </form>
    </div>
</div>
<div class="container mt-4 text-light w-100 d-flex justify-content-center">
    <h2>Mensagens</h2>
</div>
{{!-- Postagem --}}
<div class="container">
    {{#each postagem}}
    <div class="card post mt-4 gap-3">
            <div class="card cardPostForm">
                <div class="card-header">
                    <h3>{{this.titulo}}</h3>
                </div>
                <div class="body p-3">
                    <p>{{this.conteudo}}</p>
                </div>
                <div class="card-footer">
                    <small>{{this.dataCriacao}}</small>
                </div>
            </div>
            {{!-- Respostas --}}
            <div class="container mt-4 w-90">
                <div class="card contentResponses">
                    <div class="card-header">
                        nome de quem respondeu
                    </div>
                    <div class="card-body">
                        resposta
                    </div>
                    <div class="card-footer">
                        <small>data</small>
                    </div>
                </div>
            </div>
            {{!-- Form para fazer uma resposta --}}
            <div class="responses-container">
               <div class="card cardDigitResp">
                    <div class="mb-3 textarea">
                        <textarea name="" placeholder="Responda algo sobre isso..." class="form-control bg-dark text-light" id=""></textarea>
                    </div>
                    <div class="w-100 d-flex justify-content-center">
                        <button class="btn btn-success">Responder</button>
                    </div>
               </div>
            </div>
    </div>
    {{else}}
        <div class="text-light d-flex w-100 justify-content-center">
            <p>Não há nenhuma postagem por aqui... 😑</p>
        </div>
    {{/each}}
</div>


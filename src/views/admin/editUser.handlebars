<div class="container mt-4 container-edit-user">
    <div class="card">
        <div class="card-header">
            <h2>Quais dados do usuário, {{dadosUser.nameuser}}, quer editar?</h2>
        </div>
        <form action="/admin/editUser" method="post">
            <input type="hidden" value="{{dadosUser._id}}" name="id">
            <div class="card-body">
                <div class="mb-3">
                    <label for="username">Nome completo</label>
                    <input type="text" value="{{dadosUser.username}}" class="form-control" name="username" id="username">
                </div>
                <div class="mb-3">
                    <label for="nameuser">Nome de usuário</label>
                    <input type="text" value="{{dadosUser.nameuser}}" class="form-control" name="nameuser" id="nameuser">
                </div>
                <div class="mb-3">
                    <label for="useremail">E-mail (<small><i>Ao editar o E-mail do usuário ele receberá um E-mail o notificando da mudança para o novo E-mail</i></small>)</label>
                    <input type="text" value="{{dadosUser.useremail}}" class="form-control" name="useremail" id="useremail">
                </div>
                <div class="mb-3">
                    <label for="role">Função</label>
                    <select class="form-select" name="role" id="role" aria-label="Default select example">
                        <option selected>Selecionar função</option>
                        <option value="user">Usuário (apenas um usuário comum)</option>
                        <option value="admin" class="text-danger">Administrador (<strong>Quer que ele tenha alguns poderes a mais no sistema ?</strong>)</option>
                    </select>
                </div>
            </div>
            <div class="card-footer">
                <div class="mb-3 w-100 d-flex justify-content-center">
                    <button class="btn btn-success">Salvar alterações</button>
                </div>
            </div>
        </form>
    </div>
</div>